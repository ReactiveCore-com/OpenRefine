<?xml version="1.0" encoding="UTF-8"?>
<ivy-settings>
    <settings defaultResolver="main" />
    <credentials host="${artifactory.credentials.host}" realm="${artifactory.credentials.realm}"
                 username="${artifactory.credentials.user}" passwd="${artifactory.credentials.passwd}" />
    <resolvers>
        <chain name="main">
            <ibiblio name="publish" m2compatible="true" root="http://dev.reactivecore.com:8081/artifactory/libs-release-local"/>
        </chain>
    </resolvers>
</ivy-settings>